allowed_namespaces:
  - safeheal-demo

allowed_operations:
  - increase_memory_limit
  - escalate

allowed_resources:
  - memory-hog

max_memory_multiplier: 2.0

max_attempts: 2

require_human_approval:
  - increase_memory_limit
